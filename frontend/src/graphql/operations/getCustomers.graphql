query getCustomers($offset: Int! $limit:Int!) {
  paginatedCustomers(offset:$offset limit: $limit) {
    customers {
      customer_id
      first_name
      last_name
      email
    }
    totalCount
  }
}
